export const metadata = {
  title: 'RCS - Ranked Tracker',
  description: 'Auth. Carnage & Tidal',
}

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}